* text=auto

# Shell scripts must be LF for Linux containers (prevents /bin/sh\r issues)
*.sh text eol=lf

# Container and compose files are safer as LF across platforms
Dockerfile text eol=lf
**/Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Keep Windows scripts as CRLF when/if added
*.ps1 text eol=crlf
